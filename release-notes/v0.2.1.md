# MCPulse-RAG v0.2.1

Documentation and release polish update.

## Changed

- Rewrote `README_CN.md` in clean UTF-8
- Aligned both READMEs with the current Streamable HTTP endpoint and background indexing behavior
- Clarified the difference between the email delivery mainline and the `ragcheck-qqbot` branch
- Polished release documentation so public deployment defaults are easier to understand

## Notes

- No protocol or algorithm changes were introduced beyond the already released `0.2.0` behavior
